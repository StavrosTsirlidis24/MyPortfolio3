import React, {Component} from 'react'
import * as styled from './heroStyle'

class hero extends Component{
render(){
return(
<styled.container >
  <styled.heroBox>

    <styled.heroBoxTitle>
        <styled.heroBoxTitleTransparansBox>
            <styled.logoLine>
                
            </styled.logoLine>
            <h1>SIRLIDI</h1>
            <styled.logo>
                @@
            </styled.logo>
        </styled.heroBoxTitleTransparansBox>
    </styled.heroBoxTitle>

    Created to Create

  </styled.heroBox>
</styled.container>

)
    
}

}
export default hero